name: Trigger auto deployment for rag-service

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/rag-service-AutoDeployTrigger-b05bd05b-2524-42cf-9644-0fce9029fed0.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.RAGSERVICE_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.RAGSERVICE_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.RAGSERVICE_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/rag-service
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: deskmateregistry123.azurecr.io
          registryUsername: ${{ secrets.RAGSERVICE_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.RAGSERVICE_REGISTRY_PASSWORD }}
          containerAppName: rag-service
          resourceGroup: deskmate-rg_group
          imageToBuild: deskmateregistry123.azurecr.io/rag-service:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


